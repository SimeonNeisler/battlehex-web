export default class storeCard {
  constructor(name, type, deployCost, storePrice) {
    this.name = name;
    this.type = type;
    this.deployCost = deployCost;
    this.storePrice = storePrice;
  }
}
